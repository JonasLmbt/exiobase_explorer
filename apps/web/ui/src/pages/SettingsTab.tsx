import { Container, Typography } from "@mui/material";

export default function SettingsTab() {
  return (
    <Container sx={{ py: 3 }}>
      <Typography variant="body2" sx={{ opacity: 0.8 }}>
        Settings (coming soon): Theme, Defaults, Export-Optionen.
      </Typography>
    </Container>
  );
}

