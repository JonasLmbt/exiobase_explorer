# Web App (geplant)

Dieses Verzeichnis enthält die Web-Version des EXIOBASE-Explorers.

- `api/`: FastAPI Backend (HTTP API + Jobs)
- `ui/`: React Frontend (Vite)

Die PyQt5-Desktop-App bleibt im Root (`main.py`) bzw. `src/GUI/`.

